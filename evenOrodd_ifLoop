#include <stdio.h>
int main() {
    /*checking given number is even or odd */
     int a;
    scanf("%d",&a);
    {
        if(a%2==0)
        printf("given number is even %d ",a);
        else
        printf("given number is odd %d",a);
}
    return 0;
}
